<%= form_for(@iva_condition) do |f| %>
  <% if @iva_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@iva_condition.errors.count, "error") %> prohibited this iva_condition from being saved:</h2>

      <ul>
      <% @iva_condition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :observation %><br>
    <%= f.text_area :observation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
